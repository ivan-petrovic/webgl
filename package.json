{
  "name": "webgl",
  "version": "1.0.0",
  "description": "Learning WebGL and making simple engine for experimenting",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist && browserify dist/main.js --standalone Mn -o public/js/bundle.js",
    "start": "node ./index.js"
  },
  "author": "Ivan Petrovic <mangan77@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "browserify": "^16.2.2"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
